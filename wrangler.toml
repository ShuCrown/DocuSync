name = "docusync"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages 配置
pages_build_output_dir = "./dist"

# Services 绑定（代理请求到 Worker）
[[services]]
binding = "API"
service = "docusync-api"
